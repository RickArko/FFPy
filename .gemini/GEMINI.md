# GEMINI.md — FFPy

See [AGENTS.md](../AGENTS.md) for repo-wide guidance. This file exists only to
load context into Gemini CLI sessions — the canonical rules are in AGENTS.md.

## AI layer

This repo is enriched with `ai-enrich` (see `.ai-enrich.toml`). The Gemini CLI
loads context via `.gemini/settings.json`.
